# Telegram Mini App для библиотеки

Полноценное приложение для управления библиотекой с Telegram ботом и веб-интерфейсом (Mini App).

## Возможности

### Telegram Bot
- Добавление книг и читателей
- Выдача и возврат книг
- Поиск книг по автору
- Просмотр доступных книг
- Пошаговые диалоги для удобного ввода

### Mini App (Web UI)
- Современный веб-интерфейс внутри Telegram
- Просмотр всех доступных книг
- Управление читателями
- Операции выдачи и возврата книг
- Поиск по автору
- Адаптивный дизайн под Telegram темы

## Установка и запуск

### 1. Установите зависимости

```bash
mvn clean install
```

### 2. Настройте конфигурацию

Создайте файл `.env` в корне проекта:

```env
BOT_USERNAME=ваш_бот_username
BOT_TOKEN=ваш_бот_токен
```

### 3. Запустите приложение

```bash
mvn exec:java -Dexec.mainClass="ru.lostfly.Main"
```

Приложение запустит:
- Telegram бота
- REST API сервер на порту 8080
- Mini App будет доступен по адресу http://localhost:8080

### 4. Настройте Mini App в BotFather

Для работы Mini App в Telegram:

1. Откройте @BotFather в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота
4. Нажмите **Bot Settings** → **Menu Button**
5. Выберите **Edit Menu Button URL**
6. Введите URL вашего сервера (для продакшена) или используйте ngrok для локальной разработки

#### Использование ngrok для локальной разработки

```bash
# Установите ngrok: https://ngrok.com/download
ngrok http 8080
```

Скопируйте HTTPS URL из ngrok (например, `https://abc123.ngrok.io`) и укажите его в BotFather.

Также обновите URL в файле `LibraryBot.java` (строка 69):
```java
.webApp(new WebAppInfo("https://abc123.ngrok.io"))
```

## Использование

### Команды бота

- `/start` - Приветствие
- `/help` - Справка по командам
- `/webapp` или `/app` - Открыть Mini App
- `/add_book` - Добавить книгу
- `/add_user` - Добавить читателя
- `/borrow_book` - Выдать книгу
- `/return_book` - Вернуть книгу
- `/find_book_by_author` - Поиск по автору
- `/list_available_books` - Список доступных книг

### Mini App

1. Отправьте `/webapp` в чат с ботом
2. Нажмите кнопку "📱 Открыть библиотеку"
3. Используйте удобный веб-интерфейс для управления библиотекой

## Архитектура

```
├── src/main/java/ru/lostfly/
│   ├── Main.java                          # Точка входа
│   ├── components/
│   │   ├── api/
│   │   │   ├── LibraryBot.java           # Telegram бот
│   │   │   └── LibraryRestApi.java       # REST API для Mini App
│   │   ├── repository/                    # Репозитории
│   │   └── service/                       # Бизнес-логика
│   └── business/
│       ├── domain/                        # Доменные модели
│       └── handlers/                      # Обработчики
└── src/main/resources/
    └── webapp/                            # Mini App
        ├── index.html                     # HTML интерфейс
        ├── styles.css                     # Стили
        └── app.js                         # JavaScript логика

```

## REST API Endpoints

### Книги
- `GET /api/books/available` - Получить доступные книги
- `GET /api/books/search?author=name` - Поиск по автору
- `POST /api/books` - Добавить книгу
- `POST /api/books/borrow` - Выдать книгу
- `POST /api/books/return` - Вернуть книгу

### Читатели
- `GET /api/readers` - Получить всех читателей
- `POST /api/readers` - Добавить читателя

## Технологии

- **Backend**: Java 17, Javalin, TelegramBots API
- **Frontend**: HTML, CSS, JavaScript, Telegram WebApp SDK
- **Storage**: In-Memory (можно переключить на MySQL)

## Разработка

### Режимы хранения данных

По умолчанию используется in-memory хранилище. Для использования MySQL:

1. Запустите Docker:
```bash
docker-compose up -d
```

2. Измените в `Main.java`:
```java
RepositoryComponent repositoryComponent = new RepositoryComponent(
    RepositoryComponent.RepositoryMode.DATABASE
);
```

## Troubleshooting

### Mini App не открывается
- Убедитесь, что REST API сервер запущен
- Проверьте URL в BotFather и в коде
- Для локальной разработки используйте ngrok

### CORS ошибки
- Проверьте настройки CORS в `LibraryRestApi.java`
- Убедитесь, что используете HTTPS для продакшена

### Бот не отвечает
- Проверьте `.env` файл с токеном
- Убедитесь, что бот запущен в BotFather

## Лицензия

MIT